(ns adventofcode.day4-test
  (:require [clojure.test :refer :all]
            [adventofcode.day4 :refer :all]))

; I've lost work so test are disabled for now

;(deftest match-test
  ;(testing "Part 1 Does this match?"
    ;(is (not (matches? 223450)))
    ;(is (matches? 111111))
    ;(is (not (matches? 123457))))
  ;(testing "Part 2: Does this match?"
    ;(is (matches2? 112233))
    ;(is (matches2? 111122))
    ;(is (not (matches2? 123444)))))



;(deftest find-passwords-test
  ;(testing "Part 1: passwords"
    ;(is (= 1660 (find-passwords matches?))))
  ;(testing "Part 2: passwords"
    ;(is (= 1135 (find-passwords matches2?)))))


